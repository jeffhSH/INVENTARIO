<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- Faces Servlet -->
    <servlet>
        <servlet-name>Faces Servlet</servlet-name>
        <servlet-class>jakarta.faces.webapp.FacesServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <error-page>
        <exception-type>jakarta.el.PropertyNotFoundException</exception-type>
        <location>/error.xhtml</location>
    </error-page>

    <!-- Mapeo para .xhtml y .jsf -->
    <servlet-mapping>
        <servlet-name>Faces Servlet</servlet-name>
        <url-pattern>*.xhtml</url-pattern>
        <url-pattern>*.jsf</url-pattern>
    </servlet-mapping>

    <!-- Modo de desarrollo -->
    <context-param>
        <param-name>jakarta.faces.PROJECT_STAGE</param-name>
        <param-value>Development</param-value>
    </context-param>
    <context-param>
        <param-name>primefaces.THEME</param-name>
        <param-value>lara-light-blue</param-value> <!-- o lara-dark-indigo -->
    </context-param>
    <context-param>
        <param-name>primefaces.RIPPLE</param-name>
        <param-value>true</param-value>
    </context-param>

    <!-- Página de inicio opcional -->
    <welcome-file-list>
        <welcome-file>/paginas/TipoAlmacen.xhtml</welcome-file>
    </welcome-file-list>

</web-app>
